<% if @user.errors.any? %>
  <div class="alert alert-danger">
    <strong><%= t "errors.count", count: pluralize(@user.errors.count, "error") %></strong>
    <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
