<%= provide :title, @user.name %>
<div class="row">
  <div class="col-md-4">
    <div class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </div>
  </div>
  <div class="col-md-8">
    <% if @user.microposts.any? %>
      <h3><%= t "users.microposts" %> (<%= @user.microposts.count %>)</h3>
      <div class="microposts">
        <%= render @microposts %>
      </div>
      <%= will_paginate @microposts %>
    <% end %>
  </div>
</div>

