<%= provide(:title, @user.name) %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "shared/flash" %>
    <div class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </div>
  </div>
</div>
