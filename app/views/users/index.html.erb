<%= provide :title, t("users.title") %>
<div class="row">
  <div class="col-md-8 col-md-offset-3">
    <h1 class="text-center"><%= t "users.title" %></h1>
    <%= will_paginate %>
    <%= render @users %>
    <%= will_paginate %>
  </div>
</div>
